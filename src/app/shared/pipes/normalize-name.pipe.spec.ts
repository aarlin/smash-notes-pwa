import { NormalizeNamePipe } from './normalize-name.pipe';

describe('NormalizeNamePipe', () => {
  it('create an instance', () => {
    const pipe = new NormalizeNamePipe();
    expect(pipe).toBeTruthy();
  });
});
