import { FighterImagePipe } from './fighter-image.pipe';

describe('FighterImagePipe', () => {
  it('create an instance', () => {
    const pipe = new FighterImagePipe();
    expect(pipe).toBeTruthy();
  });
});
